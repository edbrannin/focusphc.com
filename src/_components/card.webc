<div class="break-inside-avoid">
  <template webc:type="11ty" 11ty:type="liquid,md">

${imagePath ? `<img src="${imagePath}">` : ''}

<h{{level}}>

{{heading}}

</h{{level}}>

<slot></slot>

  </template>
</div>
